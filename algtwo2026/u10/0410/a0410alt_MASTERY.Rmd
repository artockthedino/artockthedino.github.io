---
output: pdf_document
params:
    rseed: 1
    showsol: false
---

\renewcommand{\arraystretch}{1.2}

```{r,echo=F,warning=FALSE,message=FALSE}
options(scipen=999)
if(params$rseed !=0){
    set.seed(params$rseed)
}
knitr::opts_chunk$set(echo = FALSE)
bn <- basename(knitr::current_input())
bn = strsplit(bn,".",fixed=T)[[1]][1]

pn = function(coeffs,pows){
    pows = pows[coeffs!=0]
    coeffs = coeffs[coeffs!=0]
    ex1 = paste0("{",coeffs,"}x^{",pows,"}",collapse="+")
    ex1 = gsub("+{-","-{",ex1,fixed=T)
    ex1 = gsub("{1}x^{0}","1",ex1,fixed=T)
    ex1 = gsub("{-1}x^{0}","-1",ex1,fixed=T)
    ex1 = gsub("{1}x","x",ex1,fixed=T)
    ex1 = gsub("{-1}x","-x",ex1,fixed=T)
    ex1 = gsub("x^{0}","",ex1,fixed=T)
    ex1 = gsub("x^{1}","x",ex1,fixed=T)
    ex1 = gsub("+-","-",ex1,fixed=T)
    if(ex1 == ""){
        ex1 = "0"
    }
    ex1 = gsub("{","",ex1,fixed=T)
    ex1 = gsub("}","",ex1,fixed=T)
    return(ex1)
}
```

Name: \vspace{0.1in} \hrule

## a0410_alternative_mastery_assignment

### Question 1

Sketch the following polynomial functions. Accurately indicate the $x$ intercepts and where the curve is above or below the $x$ axis (where the function is positive or negative).

```{r,echo=F}
numq = 3
exps = character() #c("y=(x+8)(x+4)^2(x+1)(x-3)^2","y=-(x+1)^2(x-5)(x-7)")
roots = list() #list(c(-8,-4,-1,3),c(-1,5,7))
pows = list() #list(c(1,2,1,2),c(2,1,1))
degs = numeric()
as = sample(c(1,-1,sample(c(-1,1),1)))
while(length(exps)<numq){
    n = sample(3:4,1)
    while(T){
        root = sort(sample(1:8,n)*sample(c(-1,1),n,T))
        pow = sample(1:2,n,T)
        if(min(diff(root))>1 && max(diff(root))<7 && sum(pow)>n && sum(pow)<2*n){break}
    }
    daord = sample(1:n)
    # if(length(exps)>4){
    #     daord = sample(1:n)
    # }
    r2 = root[daord]
    p2 = pow[daord]
    a = as[[length(exps)+1]]
    deg = sum(pow)
    # a = ((length(exps)+1)%%2)*2-1 #sample(c(-1,1),1)
    exp1 = paste0("(x+",-r2,")^",p2,collapse="")
    exp1 = gsub("+-","-",exp1,fixed=T)
    exp1 = gsub("^1","",exp1,fixed=T)
    if(a==-1){
        exp1 = paste0("y=-",exp1)
    } else {
        exp1 = paste0("y=",exp1)
    }
    if(!(exp1 %in% exps) && !(deg%in%degs)){
        exps = c(exps,exp1)
        roots[[length(roots)+1]] = root
        pows[[length(pows)+1]] = pow
        as = c(as,a)
        degs = c(degs,deg)
    }
}


```

```{r,echo=F,fig.dim=c(6,2),results='hide'}
png("maybe.png",1200,400)
par(mar=c(0,0,0,0))
plot(0,0,"n",xlim=c(-10,10),ylim=c(-2,2),axes=F,ann=F)
abline(h=0)
abline(v=0)
for(i in -10:10){
    if(i%%5==0 && i!=0){
        lines(c(i,i),c(-0.1,0.1),lwd=3)
        text(i,-0.4,i,cex=2)
    } else{
        lines(c(i,i),c(-0.05,0.05),lwd=2)
    }
}
dev.off()
```

```{r,echo=F,fig.dim=c(6,2),results='asis',fig.align='center',eval=!(params$showsol)}
for(i in 1:length(exps)){
    cat(paste0("$",exps[i],"$\n\n"))
    cat("![](maybe.png)")
    cat("\n\n\\vfill\n\n")
    if(i%%3==0){
        cat("\\newpage\n\n")
    }
}
```

```{r,echo=F,fig.dim=c(6,2.5),results='asis',eval=params$showsol}

fx = function(x,a,root,pow){
    b = a
    for(i in 1:length(root)){
        b = b*(x-root[i])^pow[i]
    }
    return(b)
}
library(latex2exp)
for(iii in 1:length(exps)){
    exp = exps[iii]
    # cat(paste0("$",exp,"$\n\n"))
    root = roots[[iii]]
    pow = pows[[iii]]
    a = as[iii]
    N = length(root)
    par(mar=c(0,0,3,0))
    plot(0,0,"n",axes=F,ann=F,xlim=c(-10,10),ylim=c(-2,2))
    mtext(TeX(paste0("$",exp,"$")),3)
    abline(v=0,lwd=1)
    abline(h=0,lwd=2)
    for(i in -10:10){
        if(i%%5==0 && i!=0){
            lines(c(i,i),c(-0.2,0.2))
            text(i,-0.5,i,cex=0.7)
        } else {
            lines(c(i,i),c(-0.1,0.1))
        }
    }
    xs = root
    xmid = c(xs[1]-1,(xs[2:N]+xs[1:(N-1)])/2,xs[N]+1)
    sss = 1.3
    xx = xmid[1]
    yy = sign(fx(xmid[1],a,root,pow))
    mm = -yy*sss
    for(i in 1:length(root)){
        xx = c(xx,xs[i])
        yy = c(yy,0)
        if(sign(fx(xs[i]-0.1,a,root,pow))==sign(fx(xs[i]+0.1,a,root,pow))){
            mm = c(mm,0)
        }
        if(sign(fx(xs[i]-0.1,a,root,pow))==-1 && sign(fx(xs[i]+0.1,a,root,pow))==1){
            mm = c(mm,sss)
        }
        if(sign(fx(xs[i]-0.1,a,root,pow))==1 && sign(fx(xs[i]+0.1,a,root,pow))==-1){
            mm = c(mm,-sss)
        }
        xx = c(xx,xmid[i+1])
        if(i<N){
            mm = c(mm,0)
            yy = c(yy,pi/2*sign(fx(xmid[i+1],a,root,pow)))
        } else {
            mm = c(mm,sign(a)*sss)
            yy = c(yy,sign(fx(xmid[i+1],a,root,pow)))
        }
    }
    xwave = seq(min(xs),max(xs),0.1)
    sf = splinefunH(xx,yy,mm)
    lines(xwave,sin(sf(xwave)),col="blue",lwd=3)
    points(root,rep(0,length(root)),pch=19,col="blue")
    xlo = seq(min(xs)-1,min(xs),0.1)
    lines(xlo,sf(xlo),col="blue",lwd=3)
    xhi = seq(max(xs),max(xs)+1,0.1)
    lines(xhi,sf(xhi),col="blue",lwd=3)
    
    # lines(c(max(root),max(root)+2),c(0,2*a),col="blue",lwd=3)
    # lines(c(min(root),min(root)-2),c(0,2*a*(-1)^n),col="blue",lwd=3)
    
    arrows(xx[2*N+1],yy[2*N+1],xx[2*N+1]+0.5,yy[2*N+1]+0.5*sign(a)*sss,col="blue",lwd=3,length=0.1,angle=15)
    arrows(xx[1],yy[1],xx[1]-0.5,yy[1]+0.5*sign(yy[1])*sss,col="blue",lwd=3,length=0.1,angle=15)
    cat("\n\n\\vfill\n\n")
}
```

\newpage

```{r,echo=F}
library(latex2exp)
options(scipen=99)
# (x-(r+mi))(x-(r-mi))(ax+b)(cx+d)
# (x-r-mi)(x-r+mi)(ax+b)(cx+d)
# -b/a and -d/c
# (x^2-2rx+(r^2+m^2))(acx^2+(ad+bc)x+bd)
# acx^4 + (ad+bc-2rac)x^3 + (acr^2+acm^2-2rad-2rbc+bd)x^2 + (-2rbd+adr^2+adm^2+bcr^2+bcm^2)x + (bdr^2+bdm^2)
gcd2 <- function(a, b) {
  if (b == 0) a else Recall(b, a %% b)
}
gcd <- function(...) Reduce(gcd2, c(...))

rats = list(c(1,1),c(2,1),c(3,1),c(4,1),c(5,1),c(6,1),c(7,1),c(8,1),c(9,1),
            c(1,2),c(3,2),c(5,2),c(7,2),c(9,2),
            c(1,3),c(2,3),c(4,3),c(5,3),c(7,3),c(8,3),
            c(1,4),c(3,4),c(5,4),c(7,4),c(9,4),
            c(1,5),c(2,5),c(3,5),c(4,5),c(6,5),c(7,5),c(8,5),c(9,5),
            c(1,6),c(5,6),c(7,6),
            c(1,7),c(2,7),c(3,7),c(4,7),c(5,7),c(6,7),c(8,7),c(9,7),
            c(1,8),c(3,8),c(5,8),c(7,8),c(9,8),
            c(1,9),c(2,9),c(4,9),c(5,9),c(7,9))
while(T){
    rat = sample(rats,3)
    
    r = rat[[1]][1]*sample(c(-1,1),1)
    m = rat[[1]][2]*sample(c(-1,1),1)
    a = rat[[2]][1]
    b = rat[[2]][2]*sample(c(-1,1),1)
    c = rat[[3]][1]
    d = rat[[3]][2]*sample(c(-1,1),1)
    
    # acx^4 + (ad+bc-2rac)x^3 + (acr^2+acm^2-2rad-2rbc+bd)x^2 + (-2rbd+adr^2+adm^2+bcr^2+bcm^2)x + (bdr^2+bdm^2)
    A = a*c
    B = a*d+b*c-2*r*a*c
    C = a*c*r^2+a*c*m^2-2*r*a*d-2*r*b*c+b*d
    D = -2*r*b*d+a*d*r^2+a*d*m^2+b*c*r^2+b*c*m^2
    E = b*d*r^2+b*d*m^2
    cf4 = c(A,B,C,D,E)
    # (x^2-2rx+(r^2+m^2))(acx^2+(ad+bc)x+bd)
    qa = a*c
    qb = a*d+b*c
    qc = b*d
    cfq = c(qa,qb,qc)
    ca = 1
    cb = -2*r
    cc = r^2+m^2
    cfc = c(ca,cb,cc)
    if(max(abs(cf4))<100 && min(abs(c(r,m)))>1 && min(abs(c(A,B,C,D,E)))>0){break}
}

numer1 = -qb+round(sqrt(qb^2-4*qa*qc))
denom1 = 2*qa
numer2 = -qb-round(sqrt(qb^2-4*qa*qc))
denom2 = 2*qa

nn1 = numer1/gcd(c(numer1,denom1))
dd1 = denom1/gcd(c(numer1,denom1))
nn2 = numer2/gcd(c(numer2,denom2))
dd2 = denom2/gcd(c(numer2,denom2))

cn = function(real,imag){
    ss = paste0("{",real,"}+",imag,"i")
    ss = gsub("+-","-",ss,fixed=T)
    ss = gsub("+1i","+i",ss,fixed=T)
    ss = gsub("-1i","-i",ss,fixed=T)
    ss = gsub("+0i","",ss,fixed=T)
    ss = gsub("{0}+","",ss,fixed=T)
    ss = gsub("{0}-","",ss,fixed=T)
    ss = gsub("{","",ss,fixed=T)
    ss = gsub("}","",ss,fixed=T)
    return(ss)
}

frac = function(num,den){
    if(den!=1){
        ss = paste0("\\frac{",num,"}{",den,"}")
    }
    else{
        ss = paste0(num)
    }
    return(ss)
}

pn = function(c,d){
    d = d[c!=0]
    c = c[c!=0]
    p = paste0("{",c,"}x^{",d,"}",collapse="+")
    p = gsub("x^{0}","",p,fixed=T)
    p = gsub("x^{1}","x",p,fixed=T)
    p = gsub("+{-","-{",p,fixed=T)
    p = gsub("+{1}x","+x",p,fixed=T)
    p = gsub("-{1}x","-x",p,fixed=T)
    p = gsub("{-1}x","-x",p,fixed=T)
    p = gsub("{1}x","x",p,fixed=T)
    return(p)
}
```

### Question 2

```{r,echo=F}

while(T){
    nums = sample(2:10)
    p = nums[1]
    q = nums[2]*sample(c(-1,1),1)
    r = nums[3]*sample(c(-1,1),1)
    s = nums[4]*sample(c(-1,1),1)
    m = nums[5]*sample(c(-1,1),1)
    n = nums[6]*sample(c(-1,1),1)
    a = nums[7]
    b = nums[8]*sample(c(-1,1),1)
    c = nums[9]*sample(c(-1,1),1)
    # (ax^2+bx+c)(px^3+qx^2+rx+s) + (mx+n)
    # (ap)x^5 + (aq+bp)x^4 + (ar+bq+cp)x^3 + (as+br+cq)x^2 + (bs+cr+m)x + (cs+n)
    G = a*p
    H = a*q+b*p
    I = a*r+b*q+c*p
    J = a*s+b*r+c*q
    K = b*s+c*r+m
    L = c*s+n
    p5 = pn(c(G,H,I,J,K,L),5:0)
    p3 = pn(c(p,q,r,s),3:0)
    p2 = pn(c(a,b,c),2:0)
    p1 = pn(c(m,n),1:0)
    if(min(abs(c(G,H,I,J,K,L)))>0 && abs(gcd(c(a,b,c)))==1){
        break
    }
}
```


Divide $`r pn(c(G,H,I,J,K,L),5:0)`$ by the quadratic polynomial $`r pn(c(a,b,c),2:0)`$.

*Hint: your answer should look like $ax^3+bx^2+cx+d+\frac{px+q}{`r pn(c(a,b,c),2:0)`}$, where $a$, $b$, $c$, $d$, $p$, and $q$ are integers.*




\newpage

### Question 3 (multiple choice)

```{r,echo=FALSE}

cn = function(real,imag){
    ss = paste0("{",real,"}+",imag,"i")
    ss = gsub("+-","-",ss,fixed=T)
    ss = gsub("+1i","+i",ss,fixed=T)
    ss = gsub("-1i","-i",ss,fixed=T)
    ss = gsub("+0i","",ss,fixed=T)
    ss = gsub("{0}+","",ss,fixed=T)
    ss = gsub("{0}-","",ss,fixed=T)
    ss = gsub("{","",ss,fixed=T)
    ss = gsub("}","",ss,fixed=T)
    return(ss)
}

rm = sample(2:9,2)*sample(c(-1,1),2,T)

R = rm[1]
M = rm[2]

cn1 = cn(R,M)
cn2 = cn(R,-M)

ach1 = paste0("(x+",-R,"+",-M,"i)(x+",-R,"+",M,"i)",collapse = "")
ach2 = paste0("(x+",R,"+",-M,"i)(x+",R,"+",M,"i)",collapse = "")
ach3 = paste0("(x+",-R,"+",M,"i)(x+",R,"+",M,"i)",collapse = "")
ach4 = paste0("(x+",-R,"+",-M,"i)(x+",R,"+",-M,"i)",collapse = "")

ach1 = gsub("+-","-",ach1,fixed=T)
ach2 = gsub("+-","-",ach2,fixed=T)
ach3 = gsub("+-","-",ach3,fixed=T)
ach4 = gsub("+-","-",ach4,fixed=T)

ach = sample(c(ach1,ach2,ach3,ach4))
al = paste0("* $",ach,"$",collapse="\n")
sol = paste0(1*(ach1==ach),collapse="")

ff = paste0("(x-(",R,"+",M,"i))(x-(",R,"+",-M,"i))",collapse = "")
ff = gsub("+-","-",ff,fixed=T)

```

Which of the quadratic polynomials below has a root of $`r cn1`$ and real coefficients (when in standard expanded form)?

```{r,echo=F,results='asis'}
for(i in 1:4){
    cat(paste0(letters[i],". $",ach[i],"$\n"))
}

```

\vspace{0.5in}

### Question 4 (multiple choice)

```{r,echo=FALSE}
pn = function(c,d){
    d = d[c!=0]
    c = c[c!=0]
    p = paste0("{",c,"}x^{",d,"}",collapse="+")
    p = gsub("x^{0}","",p,fixed=T)
    p = gsub("x^{1}","x",p,fixed=T)
    p = gsub("+{-","-{",p,fixed=T)
    p = gsub("+{1}x","+x",p,fixed=T)
    p = gsub("-{1}x","-x",p,fixed=T)
    p = gsub("{-1}x","-x",p,fixed=T)
    p = gsub("{1}x","x",p,fixed=T)
    return(p)
}

cn = function(real,imag){
    ss = paste0("{",real,"}+",imag,"i")
    ss = gsub("+-","-",ss,fixed=T)
    ss = gsub("+1i","+i",ss,fixed=T)
    ss = gsub("-1i","-i",ss,fixed=T)
    ss = gsub("+0i","",ss,fixed=T)
    ss = gsub("{0}+","",ss,fixed=T)
    ss = gsub("{0}-","",ss,fixed=T)
    ss = gsub("{","",ss,fixed=T)
    ss = gsub("}","",ss,fixed=T)
    return(ss)
}

rm = sample(2:9,2)*sample(c(-1,1),2,T)

R = rm[1]
M = rm[2]

cn1 = cn(R,M)
cn2 = cn(R,-M)

start = paste0("(x+",-R,"+",-M,"i)(x+",-R,"+",M,"i)",collapse = "")
start = gsub("+-","-",start,fixed=T)

## (x-R-Mi)(x-R+Mi)
# x^2 - 2*Rx + R^2+M^2

ach1 = pn(c(1,-2*R,R^2+M^2),2:0)
ach2 = pn(c(1,2*R,R^2+M^2),2:0)
ach3 = pn(c(1,-2*R,abs(R^2-M^2)),2:0)
ach4 = pn(c(1,2*R,abs(R^2-M^2)),2:0)

ach = sample(c(ach1,ach2,ach3,ach4))
al = paste0("* $",ach,"$",collapse="\n")
sol = paste0(1*(ach1==ach),collapse="")
```

Expand the quadratic polynomial with complex roots into standard form.

$$`r start`$$

`r paste0(letters[1:4],". $",ach,"$",collapse="\n")`

\vspace{0.5in}

### Question 5

```{r,echo=F}
# (px+q)(mx+n)
# x = -q/p  or -n/m
# pmx^2 + (pn+qm)x + qn
gcd2 <- function(a, b) {
  if (b == 0) a else Recall(b, a %% b)
}
gcd <- function(...) Reduce(gcd2, c(...))

while(T){
    pqmn = sample(2:9,4)
    p = pqmn[1]
    q = pqmn[2]*sample(c(-1,1),1)
    m = pqmn[3]
    n = pqmn[4]*sample(c(-1,1),1)
    A = p*m
    B = p*n+q*m
    C = q*n
    if(abs(gcd2(p,q))==1 && abs(gcd2(m,n))==1 && max(abs(c(A,B,C)))<20){
        break
    }
}

```

Use the quadratic formula to express both rational solutions (as simplified fractions). 

$$`r pn(c(A,B,C),2:0)`~=~0$$

*Hint: The quadratic formula says if $ax^2+bx+c=0$ then $x=\frac{-b\pm\sqrt{b^2-4ac}}{2a}$.*


